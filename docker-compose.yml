services:
  web:
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      - db
    volumes:
      - ./addons:/mnt/extra-addons

  db:
    image: postgres:13
    env_file:
      - .env
